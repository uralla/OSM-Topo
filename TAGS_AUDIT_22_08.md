# TAGS_AUDIT.md — Аудит стиля Garmin `uralla` (mkgmap)

**Статус:** рабочий документ, режим аудита. Ничего из перечисленного ниже не внесено
в реальные файлы стиля — это накопленный список решений/находок/открытых вопросов
для дальнейшего внедрения.

**Контекст проекта:** карта НЕ является generic-стилем. Специализация — лесной пеший
и велосипедный туризм (регион — предположительно Урал, судя по `landuse_points` и
названию `uralla`). Автор карты не программист, стиль собирался годами из разных
источников, отсюда накопленные несостыковки. Параллельно ведётся обсуждение с ChatGPT
(через API, история частично утеряна из-за лимитов) и Qwen3 (локально). Этот файл —
попытка синхронизировать три параллельных ревью в одну точку правды.

**Роль каждого участника:** независимо сверять с реальными файлами стиля/TYP, не
принимать выводы других моделей на веру, различать: confirmed bug / likely problem /
optimization / aesthetic choice / unresolved question. Не переписывать уже
зафиксированные решения пользователя без новых доказательств.

---

## 1. Подтверждённые баги (проверено построчно по файлам)

| # | Файл:строка | Проблема | Статус |
|---|---|---|---|
| 1 | `points:178-182` | `amenity=fuel [0x2f01 resolution 24]` без `continue` — перехватывает всё, правила для `fuel:HGV_diesel=yes` (0x2f16) и `shop=convenience` (0x2e06) никогда не срабатывают | **confirmed bug**, нужно добавить `continue` на 178 |
| 2 | `lines:589,594` | Списки «(гравийка)» / «(плохая гравийка)» содержат `surface=paved` — асфальт подписывается как гравий | **confirmed bug**, убрать `surface=paved` из обоих списков |
| 3 | `points:263,442` | `historic=memorial {name '${inscription}'}`, `amenity=signpost {name '${label}'}` — без фолбэка дают пустое имя POI | **confirmed bug**, нужны цепочки `\| 'памятник'` / `\| 'указатель'` |
| 4 | `points:23` | `opening_hours=*` подмешивается в `addr:postcode` | **вероятно намеренный** приём (известный в комьюнити mkgmap способ показать доп. инфо в адресном поле POI на Garmin) — не удалять бездумно, но задокументировать явно, т.к. портит адресный индекс |
| 5 | `points:365` | `shop=kiosk [0x2e00]` — полностью избыточно, ниже уже есть catch-all `shop=* & shop!=no & shop!=none [0x2e00]` | **confirmed bug** (мёртвый код), удалить строку 365 |
| 6 | `points:2e08` | `shop=chemist` (аптека) и `shop=garden_centre` делят один тип с MPC Name `HOME_AND_GARDEN_STORE`, а `shop=optician` использует тип с MPC Name `PHARMACY` (`0x2e05`) — семантика перепутана | **confirmed problem**, решение ниже в разделе 4 |

## 2. Хаки, которые выглядят как баги, но по факту — не совсем

| Место | Что смущает | Что оказалось при проверке |
|---|---|---|
| `lines:609` `highway=* & tunnel=yes {add name='туннель'}` | Похоже на no-op | `add` в mkgmap = «установить, если тег ещё не установлен» → рабочий приём: даёт дефолтное имя безымянным туннелям, не no-op |
| `inc/water_lines:7-14` `taxi=yes` на реках | Похоже на грязный роутинг-хак | Комментарий в коде: «роутинг по рекам — линия с синими стрелками направления течения». Вероятно осознанная фича — маршрутизация для сплава/рафтинга через слабоиспользуемый профиль `taxi`. **Не удалять**, но протестировать реальный маршрут на приборе/BaseCamp прежде чем менять |
| `points:380-395` `building=yes/true & mkgmap:area2poi!=true → 0x2b07` (тип «изба») | Изначально казалось «все здания → избушки», один из самых опасаемых пунктов | `points`-файл обрабатывает только узлы (nodes), не полигоны. Обычные здания (way-полигоны) корректно рисуются в `polygons:40` как `0x13`, это правило их не касается. `mkgmap:area2poi=true` появляется только при включённой опции `--add-pois-to-areas`, которая сейчас закомментирована в обоих `.args`. **Реальный текущий эффект ограничен** (задевает только редкие настоящие point-теги building=yes). Но: (а) семантически неверно — не «изба», (б) скрытая зависимость от опции, которая может быть включена в будущем и тогда неожиданно испортит карту. **Confirmed problem, но не urgent** — убрать из цепочки `0x2b07`, задокументировать `[IMPORTANT]` |
| `version` файл = `1`, `info` файл `version=3.0` | Ранее (в другом ревью) расценено как несоответствие для исправления | `version` — версия ФОРМАТА style-файла в спецификации mkgmap (не про сам стиль), `info: version=3.0` — версия стиля автора. Два разных смысла, **синхронизировать не нужно**, `version=1` оставить как есть |

## 3. Устаревшие/legacy OSM-теги — что уже обработано правильно, что нет

### Уже обработано защитным образом (подтверждено, трогать не надо)
- `highway=unsurfaced` (deprecated с 2007-2008, замена — `surface=unpaved`) → `lines:52,214` уже конвертирует в `track` + `mkgmap:unpaved=1`
- `ford=yes` / `highway=ford` — оба варианта покрыты параллельно в `lines` и `points`
- `leisure=nature_reserve` / `landuse=nature_reserve` / кривые формы `natural_reserve` — уже все 4 варианта вместе в `polygons:100`
- `landuse=cemetary` (опечатка) / `landuse=cemetery` — оба варианта через `|` в `inc/landuse_polygons:5-6`

### Найденные пробелы (тега нет вообще в стиле, хотя используется мапперами)
Проверено через OSM Wiki (не по памяти — она тоже может быть неактуальной):

| Тег | Статус проверки | Комментарий |
|---|---|---|
| `boundary=protected_area` (+`protect_class=*`) | подтверждено, современная схема охраняемых территорий (нацпарки/заповедники) взамен одиночного `leisure=nature_reserve` | **confirmed gap** — если заповедник в OSM размечен этим способом, на карте не появится вообще. Добавить параллельно в `polygons:100`, `lines:237`, `points:275` |
| `boundary=national_park` | не проверено отдельно | тоже отсутствует, стоит проверить есть ли нацпарки в целевом регионе с этим тегом |
| `information=guidepost` (+обязательный `tourism=information`) | подтверждено, de facto standard для путевых указателей на туристических/вело маршрутах | **confirmed gap**, отдельный от `amenity=signpost`. Сейчас `tourism=information` есть одним generic-типом — можно оставить как есть или выделить подтип |
| `historic=wayside_cross` | подтверждено, включая RU-версию Wiki: «встречается вдоль дорог в России» | **confirmed gap** — поклонные кресты, реальные навигационные ориентиры в лесу, тега нет вообще |
| `historic=wayside_shrine` | подтверждено | **confirmed gap** — придорожные часовенки/киоты, аналогично |
| `summit:cross=yes` | подтверждено как атрибут к `natural=peak`/`man_made=cross` | бонус, не критично |
| `natural=saddle` | **не подтверждено поиском**, была лишь гипотеза по памяти | требует отдельной проверки, не включать в работу как факт |

### Методика на будущее (согласовано с пользователем)
Идея пользователя: завести `inc/normalize_deprecated` — единая точка ре-маппинга
старых тегов в новые, подключаемая первым `include` во все геометрические файлы
(`points`/`lines`/`polygons`). Технически возможно — в стиле уже есть прецедент
переиспользуемых `include` (`inc/name` подключён трижды).

**Важные ограничения этого подхода** (проговорено, важно не забыть при реализации):
1. Не все замены 1:1 — например `boundary=protected_area` несёт доп. смысл
   (`protect_class`), которого нет у `leisure=nature_reserve`; для таких случаев
   правильнее не переписывать тег, а расширить условие в самом правиле отрисовки
   (как уже сделано для nature_reserve), а не нормализовывать.
2. Использовать `add`, не `set` — чтобы не затирать уже корректный новый тег, если
   оба тега почему-то сосуществуют на одном объекте: `oldtag=value & newtag!=* { add newtag=... }`
3. Подключать файл нормализации нужно во все geometry-файлы, где тег может
   встретиться (например shop-теги — это и `points`, и `polygons` для зданий-магазинов).

Пока реальных кандидатов на 1:1-нормализацию (кроме уже обработанной опечатки
cemetary/cemetery) не найдено — файл заводить рано, копить кандидатов по ходу
дальнейшего аудита.

## 4. Согласованные решения по консолидации POI (готово к внедрению)

### Еда → один тип
Заменяет `points:150-234` (27 правил, 20 типов `0x2a00-0x2a13`):
```
amenity=restaurant | amenity=cafe | amenity=fast_food | amenity=food_court [0x2a00 resolution 24]
```
Основание пользователя: данные на карте не всегда точны, детализация кухни не нужна;
название заведения всё равно видно в подписи; карта — для ориентирования, не точного
подбора, «для этого есть телефон».
**TYP на удаление:** `0x2a01-0x2a13` (19) + уже мёртвые `0x2a14-0x2a17` (4) = 23 типа.

### Продукты → один тип (включая мясную лавку)
Заменяет `points:329-363`:
```
shop=bakers | shop=bakery | shop=butcher | shop=convenience | shop=general | shop=organic | shop=supermarket {name '${name}'} [0x2e02 resolution 24]
```
**TYP на удаление:** `0x2e03`, `0x2e0a`, `0x2e0c`.

### Аптека — оставить отдельно (важно для безопасности)
```
shop=chemist [0x2e05 resolution 24]
```
Тип `0x2e05` (MPC: PHARMACY) — исправление найденной путаницы (см. раздел 1, п.6).

### Оптика — не выделять отдельно
Уходит в общий тип «прочие магазины» вместе со всем остальным непродуктовым.
*(Уточнение статуса: пользователь написал «можно смело убирать» — трактуется как
«не давать отдельный значок», НЕ «скрыть с карты полностью». Если имелось в виду
второе — требуется явное подтверждение.)*

### Велосипед — оставить отдельно (приоритет карты — вело-туризм)
```
shop=bicycle [0x2f13 resolution 24]
```
Мотозапчасти (`shop=motorcycle`) — НЕ сюда, уходят в авто-группу.

### Все остальные непродуктовые магазины → один общий тип
Заменяет `points:329-367` целиком (за вычетом продуктов/аптеки/велосипедов выше):
```
shop=* & shop!=no & shop!=none [0x2e00 resolution 24]
```
Покрывает: beauty, hairdresser, boat, car*, car_dealer, car_parts, car_rental,
car_repair, car_wrecker, tires/tyres, motorcycle, optician, clothes, fashion, shoes,
department_store, mall (объединены по решению пользователя), doityourself, furniture,
hardware, garden_centre и т.д.

**Порядок правил важен** — продукты/аптека/велосипеды должны идти РАНЬШЕ общего
catch-all правила (mkgmap берёт первое совпадение без `continue`).

**TYP на удаление (доп. к найденному по еде/продуктам):** `0x2f10`, `0x2f09`, `0x2f0d`,
`0x2f03`(сохранить под общий тип авто, либо тоже в 0x2e00 — уточнить), `0x2f07`,
`0x2f02`, `0x2f0a`, `0x2e08`, `0x2e07`, `0x2e01`+`0x2e04`, `0x2e09` — суммарно
**~37 TYP-слотов освобождается по всей группе еда+продукты+магазины**.

### Деревья/кусты — POI только для именованных
```
natural=tree & name=* ... (добавить условие name=* к существующим правилам inc/landuse_points:90-91)
natural=shrub & name=* ...
```
Безымянные — не должны порождать POI вообще.

### Опоры ЛЭП — убрать POI полностью
`points:297` `man_made=utility_pole [0x11506]` — удалить. Линии `power=line`/
`power=minor_line` не затрагиваются, остаются как есть.

## 5. TYP — найденные несоответствия MPC Name ↔ картинка (для справки, не срочно)

Все три примера ниже сейчас НЕ используются активными правилами (мёртвый код),
поэтому не горит, но это хорошие иллюстрации методологии на будущее:
- `0x1351d` — MPC: *RESTAURANT_PUNJABI*, авторские строки: «шиномонтаж»/«tyre service»
- `0x1351e` — MPC: *RESTAURANT_RAJASTHANI*, авторские строки: «Озон»/«» (маркетплейс)
- `0x13703` — MPC: *TAXI_STAND*, авторские строки: «горячий источник»/«hot spring»

**Договорённость с пользователем по методике чистки TYP:** сначала правильный
Type/SubType (для разумного fallback на встроенный Garmin-значок, если TYP не
подхватится на устройстве), потом объединение одинаковых картинок, потом дизайн.
Не наоборот. Ориентир для сверки типов — список из TYPViewer (программа, которой
пользователь редактирует TYP). Тест на реальном приборе — Garmin GPSMap 67
(нюанс: с прошивки ~8.80 у 67-й есть системная Etyp-прошивка, которая может
подмешиваться поверх стороннего TYP — при неожиданном виде символов сначала
исключить это, а потом уже грешить на свой TYP). Для быстрых итераций пользователь
использует QMapShack (свой рендерер, обычно точен, но не 100% идентичен
прошивке/BaseCamp — для критичных вещей сверять с реальным прибором).

**Полный граф `points/lines/polygons ↔ TYP` уже построен** (33 мёртвых TYP-записи,
17 активных правил без TYP-определения — см. историю чата, при необходимости
пересчитать скриптом `parse_typ.py`/`cross_reference3.py`).

## 6. Прочие находки (из отдельного независимого ревью, не полностью проверено вживую)
*(из более раннего REVIEW.md/REVIEW_URALLA_MASTER.md, часть уже покрыта выше)*
- `inc/access`/`inc/compat_lines`/`points`/`finalize` — логика access/barrier размазана по нескольким местам, дублируется. Согласован принцип «одна задача — одно место», конкретный план рефакторинга не составлен.
- `56 жёстко закодированных имён` (реки, вершины, `ref=М-5`) — предложение заменить на пороговые условия (`ele>=N`, `width>=N`) вместо списков имён. Не реализовано, требует решения пользователя по порогам.
- `make-cycleways` в `uralla.args` — подтверждено через официальную документацию mkgmap: **obsolete-опция, не имеет эффекта** в текущих версиях. Безопасно убрать.
- `cycle-map` в `uralla.args` — реальная опция, но офиц. документация прямо предупреждает «Don't use with the default style as that is a general style!». Стиль `uralla` унаследован от старого default-стиля mkgmap → требуется целенаправленное тестирование маршрутизации, не факт что это баг, но и не факт что безопасно.
- CRLF (Windows line endings) по всем файлам стиля — не проблема сама по себе, но будет шуметь в git diff при правках на других ОС. Решить сознательно (`.gitattributes`) один раз, не смешивать построчно.
- `relations`, `inc/access_country`, `inc/address` содержат правила для нерелевантных стран/сетей (US:I, US:US, AsianHighway, TAH, BEL/NLD/ESP и т.д.) — кандидаты на чистку, низкий приоритет.

## 7. Реструктуризация файлов (согласованный план, не реализовано)

Согласовано с пользователем: разбить `lines`/`points`/`polygons` на смысловые
секции (NORMALIZE → SPECIAL → TRANSPORT-группы → LABELS → FINALIZE для lines;
аналогично для points), **сохраняя порядок правил внутри секций** (функционально
важен из-за `continue`). НЕ сортировать алфавитно — только по смыслу.

**Комментарии — двуязычные (русский + английский), с тремя маркерами:**
- `[IMPORTANT/ВАЖНО]` — для критической логики (пример: building=yes→hut защита от add-pois-to-areas)
- `[INTENTIONAL/НАМЕРЕННО]` — для осознанных нестандартных решений (пример: taxi=yes на реках)
- `[CUSTOM/АВТОРСКОЕ]` — для авторских картографических решений

Реструктуризация — отдельный этап **после** логической чистки, не одновременно с ней.

## 4a. Обновление по shop= (финальное решение, уточняет раздел 4)

- `shop=chemist` (аптека) — отдельно, `0x2e05` (MPC: PHARMACY, теперь правильно)
- `shop=optician` — без отдельного значка, уходит в общий catch-all (не путать с «скрыть с карты полностью» — просто без своей иконки)
- `shop=bicycle` — отдельно, `0x2f13` (приоритет карты — вело)
- `shop=motorcycle` — НЕ с велосипедом, уходит в авто-группу
- Авто-группа (`car`, `car_dealer`, `car_parts`, `car_rental`, `car_repair`, `car_wrecker`, `tires`, `tyres`, `motorcycle`) — один тип `0x2f03`
- `department_store` + `mall` — объединены, тоже в общий catch-all (не отдельным типом)
- Общий catch-all `shop=* & shop!=no & shop!=none [0x2e00]` — все остальные непродуктовые (beauty, hairdresser, boat, optician, clothes, fashion, shoes, doityourself, furniture, hardware, garden_centre, department_store, mall и т.д.)
- `shop=kiosk` (points:365) — удалить, дублирует catch-all

## 9. Аудит по смысловым группам тегов (систематический проход)

Методология: для каждой группы — (1) вытащить текущее покрытие из файлов стиля,
(2) сверить с актуальным статусом тегов на OSM Wiki (не полагаться на память —
она тоже может быть устаревшей), (3) найти реальные пробелы, (4) для найденного —
подобрать свободный TYP-слот по методике «MPC Name соответствует смыслу» там, где
это осмысленно (custom-слоты с пустым MPC Name — свободны для любого переиспользования).

### barrier=*
- `barrier=motorcycle_barrier` + `barrier=cattle_grid` → иконка «шлагбаум»
  **[TODO: не зафиксирован конкретный числовой код TYP-слота — уточнить у пользователя, какой именно "шлагбаум" (возможно `0x5800`/`0x5801`, уже используются под `barrier=gate`/`barrier=lift_gate` — уточнить, тот же слот или другой)]**
- `barrier=motorcycle_barrier` — access по умолчанию **[открытый вопрос]**: задавать ли `bicycle=yes; foot=yes; access=no` как дефолт (по аналогии с `bollard`/`cycle_barrier` в `inc/access:81-86`), или не предполагать проезд на велосипеде без явного тега — решение не принято
- `barrier=cattle_grid` — без ограничений доступа (чисто информационный POI, не блокирует никого)
- `barrier=log | obstacle=fallen_tree | barrier=debris` → `0x6602` (свободный слот «камни», требует новой картинки в TYPViewer — завал, не камни)

### natural=* (рельеф)
- `natural=saddle` → тот же тип, что `mountain_pass=yes` (`0x11507`), тот же паттерн подписи `name`/`ele`/`rtsa_scale`
- `natural=valley` → `0x6617` (MPC: VALLEY, точное совпадение)
- `natural=arch` → `0x6601` (MPC: ARCH, точное совпадение — технически точный вариант, выбран пользователем осознанно)
- `natural=cave_entrance` → перенесён на `0x11602` (custom-слот с пустым MPC, освобождён от arch; **требует новой картинки в TYPViewer**)
- `sport=climbing` → `0x6607`, тот же тип, что `natural=rock`/`natural=cliff`

### emergency=* (безопасность)
- `amenity=emergency_phone | emergency=phone` → `0x2f12` (deprecated-тег, добавлен параллельно, не заменён)
- `emergency=defibrillator` → добавить **[TODO: слот не подобран]**
- `emergency=first_aid_kit` → добавить **[TODO: слот не подобран]**
- `emergency=fire_hydrant` — сознательно ИСКЛЮЧЕНО (городская инфраструктура для пожарных, нерелевантно)
- `emergency=assembly_point` — сознательно ИСКЛЮЧЕНО (городская эвакуационная логика, нерелевантно)

### man_made=*
- `man_made=cairn` (тур/пирамидка-ориентир) → `0x2f18` (custom-слот с пустым MPC, был «интернет»)

### Источники воды
Объединено в `inc/water_points:17`, с учётом `intermittent`/`seasonal`:
```
man_made=water_well | man_made=water_tap | amenity=drinking_water | natural=spring
    & (intermittent!=yes & !(seasonal=* & seasonal!=no)) {addlabel 'вода'} [0x6511 resolution 22]

man_made=water_well | man_made=water_tap | amenity=drinking_water | natural=spring
    & (intermittent=yes | (seasonal=* & seasonal!=no)) {addlabel 'вода (пересых.)'} [0x6512 resolution 23]

amenity=water_point {addlabel 'запас воды'} [0x5001 resolution 23]
```
Решение пользователя: `seasonal=*` (кроме `no`) автоматически трактуется как
`intermittent=yes` — без попытки выводить конкретный сезон текстом (упрощение,
т.к. точность разметки сезонности в реальных данных не гарантирована).

### route=hiking / route=bicycle / route=mtb (relations) — КРУПНЕЙШАЯ находка сессии
Полностью отсутствовала обработка промаркированных туристических/вело маршрутов
(relations). Вся существующая `route=*` логика — только городской транспорт и
автодороги (US/AsianHighway/TAH/e-road), нерелевантные для этой карты.

**Связь с найденным ранее:** пустой файл `overlays` (0 байт, ранее помечен как
«мусор на удаление») — это на самом деле штатный mkgmap-механизм именно для
подсветки промаркированных маршрутов (hiking/mtb/bicycle relations). Официальная
документация mkgmap: *«Can be used to highlight mtb, bicycle, foot or other
relations»*.

**Реализовано (минимум, согласовано с пользователем):**
```
# relations
type=route & route=hiking & name=* {
  apply { set mkgmap:trail_name='$(mkgmap:trail_name);${name|not-contained:;:mkgmap:trail_name}' | '$(mkgmap:trail_name)' | '${name}'; }
}
type=route & (route=bicycle | route=mtb) & name=* {
  apply { set mkgmap:trail_name='$(mkgmap:trail_name);${name|not-contained:;:mkgmap:trail_name}' | '$(mkgmap:trail_name)' | '${name}'; }
}

# lines
mkgmap:trail_name=* & name!=* { set name='${mkgmap:trail_name}' }
```
Плюс дополнительная идея пользователя —way, входящие в маркированный маршрут,
показывать «на ступень дальше» (раньше видны при отдалении), без новой графики
в TYP — просто более широкое окно `resolution`. Требует ~6 точек вставки в `lines`
(`footway`, `cycleway`, `path`×2 ветки, `track`×2 ветки) + создание окна `resolution`
для `bridleway` с нуля (сейчас у него нет разбивки по масштабам вообще, только
`resolution 24`). Порог `length()` — предложено `>100`, по аналогии с уже принятым
в файле решением для `track & tracktype=grade1`. **[TODO: не реализовано построчно,
только согласован принцип — требует отдельного прохода по всем 6+1 точкам вставки]**

**Отложено на будущую версию:** визуальная подсветка через `overlays`
(новый line-тип в TYP, дифференциация `network=lwn/rwn/nwn`) — нужно тестировать
на конкретных реальных маршрутах в регионе, не делать вслепую.

### surface=* / smoothness=* (дорожный блок, `lines` finalize)
Актуальный канонический список (OSM Wiki, `Template:Map_Features:surface`):
мощёное — asphalt/concrete/concrete:lanes/concrete:plates/paving_stones/sett/
unhewn_cobblestone/cobblestone/cobblestone:raised/metal/wood/tiles; немощёное —
compacted/fine_gravel/gravel/pebblestone/dirt/earth/grass/grass_paver/gravel_turf/
ground/mud/sand/woodchips/snow/ice; спец — clay/tartan/artificial_turf/decoturf/metal_grid.

- «грунтовка»/«плохая грунтовка» — добавить `dirt`, `earth`, `woodchips`
- `surface=snow` → объединить с существующей веткой `winter_road=yes`
- `surface=ice` → объединить с существующей веткой `ice_road=yes`
- Отдельную категорию snow/ice сознательно решили НЕ делать (упрощение)

**Confirmed problem, исправлено:** полная шкала `smoothness` — 8 ступеней
(`excellent → good → intermediate → bad → very_bad → horrible → very_horrible → impassable`,
подтверждено OSM Wiki). Декоративные оверлеи битых дорог (`lines:79,81`) покрывали
только 4 из 8 ступеней, причём тип `0x1001b` (авторская строка в TYP буквально
**«плохая»**) реально триггерился только от `very_bad`, пропуская саму ступень `bad`.
Исправлено:
```
highway!=path & highway!=footway & highway!=cycleway & highway!=bridleway & highway!=steps & highway!=pedestrian
    & ( smoothness=very_horrible | smoothness=impassable | smoothness=horrible ) [0x1001a resolution 24 continue]
highway!=path & highway!=footway & highway!=cycleway & highway!=bridleway & highway!=steps & highway!=pedestrian
    & ( smoothness=bad | smoothness=very_bad ) [0x1001b resolution 24 continue]
```
Список исключений расширен по объяснению пользователя: доп. графика нужна только
для типов дорог, проезжих для машин (машины — основная причина «убитого»
покрытия), пешеходные/велосипедные типы исключены полностью, не только `path`.

### piste:type=*
- `piste:type=nordic` (беговые лыжи) → `0x10101`, тот же тип, что `skitour`

### leisure=*
- `leisure=picnic_table` (одиночный стол, в отличие от целого `tourism=picnic_site`) → `0x4a01`

### boundary=*
- `boundary=protected_area` (+`protect_class=*`) и `boundary=national_park` — добавлены в полигон (`area_size()>50000`, тот же порог, что у `nature_reserve`) и в линию-границу (`0x12d1b`, без порога площади)
- Отдельная POI-точка для заповедников — **сознательно НЕ добавляется** (решение пользователя: граница уже даёт понять «пересёк/не пересёк», точка при этом только одна на большой объект с потенциально многими входами, поиск по имени не требуется для этой категории)
- `boundary=national` — по данным самой OSM Wiki: *«no clear meaning, usage appears to be a mix of various mistakes»*. Вероятно, простаивающий код в `lines:479`. Не трогать без решения пользователя, просто зафиксировано
- `boundary=political` — используется для электоральных округов, нерелевантно для карты, вероятно тоже простаивает (`lines:480`)

### Вело-инфраструктура (amenity=*, приоритет карты)
- `amenity=bicycle_repair_station` (бесплатный инструмент+насос) → `0x2e0d`
- `amenity=bicycle_rental` → добавить **[TODO: слот не подобран]**
- `amenity=bicycle_parking` → добавить **[TODO: слот не подобран]**
- Отличие от уже существующего `shop=bicycle`: это некоммерческая инфраструктура, не магазин

### aerialway=*
Пробелов не найдено. `aerialway=zip_line` уже покрывается существующим catch-all
`aerialway=* [0x10f15]` — падает в общий тип без отдельной подписи, что приемлемо.

### amenity=* (остальные, широкий обзор)
Покрытие уже очень плотное (74 разных значения). Единственный правдоподобный
кандидат: `amenity=vending_machine` (+`vending=drinks`/`vending=food`) — устоявшийся,
стабильный тег, полезен на отдалённых заправках/вокзалах. **[Предложено, ожидает
подтверждения пользователя]**

## 8. Открытые вопросы (требуют решения пользователя)

- [ ] `natural=saddle` — актуальность тега не подтверждена поиском, требует проверки
- [ ] `boundary=national_park` — есть ли нацпарки в целевом регионе с этим тегом?
- [ ] `information=guidepost` — выделять отдельным типом или достаточно generic `tourism=information`?
- [ ] `optician` — «убрать» значит «без своего значка» или «вообще скрыть с карты»?
- [ ] `0x2f03` при объединении авто-группы — использовать как итоговый тип для всей авто-группы, или тоже увести в общий `0x2e00`?
- [ ] `0x10106`/`0x10107` («велодорожка»/«велотропа») в TYP — есть готовая графика, но нигде не подключено в правилах. Заготовка на будущее или забытый кусок?
- [ ] Жёстко закодированные списки имён (реки/вершины/`М-5`) — какие пороги использовать для замены на условия?
- [ ] Числовой код TYP-слота «шлагбаум» для `motorcycle_barrier`/`cattle_grid` — не зафиксирован
- [ ] Access-дефолт для `barrier=motorcycle_barrier` (предполагать ли `bicycle=yes` без явного тега?)
- [ ] TYP-слоты для `emergency=defibrillator`, `emergency=first_aid_kit`, `amenity=bicycle_rental`, `amenity=bicycle_parking` — не подобраны
- [ ] Подтверждение на `amenity=vending_machine` (drinks/food) — предложено, не подтверждено
- [ ] Реализация «маркированный маршрут — на ступень дальше видимости» — согласован принцип и порог `length()>100`, не расписано построчно по всем ~7 точкам вставки (`footway`/`cycleway`/`path`×2/`track`×2/`bridleway`-с-нуля)
- [ ] Новая графика в TYPViewer для переехавших/новых слотов: `0x6602` (было «камни» → «завал»), `0x11602` (было нейтральное → «пещера»)

## 10. Следующие шаги
Пройдено систематически (см. раздел 9): shop-группа (финал), barrier, natural-рельеф,
emergency, man_made=cairn, источники воды, route=hiking/bicycle/mtb (relations),
surface/smoothness, piste, leisure=picnic_table, boundary, climbing, вело-инфраструктура,
aerialway, amenity (общий обзор).

Не пройдено ещё: `craft=*`, `office=*`, `waterway=*` (детальный пробельный анализ,
не только багфиксы), `landuse=*` (детальный пробельный анализ), `railway=*`
(детальный пробельный анализ). Приоритет и порядок — по решению пользователя,
методология та же: текущее покрытие → сверка с актуальным статусом на OSM Wiki →
пробелы → решение.
